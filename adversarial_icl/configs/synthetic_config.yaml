# Configuration for synthetic experiments

experiment1:
  d: 20           # Input dimension
  m: 16           # Model capacity (attention head dimension)
  N: 15           # Context sample size

experiment2:
  d: 20           # Input dimension
  N: 15           # Context sample size
  epsilon: 0.5    # Tolerable risk increment

experiment3:
  d: 20           # Input dimension
  m: 16           # Model capacity
  N0: 5           # Baseline sample size

training:
  pretrain_steps: 5000
  batch_size: 32
  learning_rate: 0.001

pga:
  iterations: 50
  batch_size: 16
  learning_rate: 0.05